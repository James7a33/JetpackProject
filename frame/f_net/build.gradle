plugins {
    alias(libs.plugins.android.library)
}

apply from: '../../common.gradle'

android {
    namespace 'com.frame.net'
}

dependencies {
    api project(':frame:f_base')

    api config.okhttp3
    api config.rxhttp
    kapt config.rxhttpCompiler
}